# v4 开发状态

**v4 网页拟合已启用，在浏览器本地运行共享 C++ 核心。** 测量数据上传、时域分析、扫频、实时监测和实验历史继续可用。

拟合页保留 CSV/示例/历史导入、元件输入和图编辑器，执行按钮暂时禁用。
新算法既可通过 `AlgorithmLcr/` 的 C++ 命令行运行，也可在网页三个 Try 栏目中运行。
旧网页计算包已删除，不会悄悄调用旧算法。

| 模式 | 已知信息 | 输出 |
|---|---|---|
| Try1 | 可选规范等效器件数 | 有界串并联假设内的候选类 |
| Try2 Exact | 类型、数量、精确参数 | 候选接线 |
| Try2 Tolerance | 类型、数量、标称参数、显式容差 | 接线及容差内参数 |
| Try3 | 图与边类型 | 可辨识聚合群的参数 |

L 与串联 DCR 算一个器件。单端口行为一般不能唯一确定内部物理接线。
`exactN` 指归约后的等效模型数量，不代表真实物理封装数量。
